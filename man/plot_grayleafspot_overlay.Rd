% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overlay.R
\name{plot_grayleafspot_overlay}
\alias{plot_grayleafspot_overlay}
\title{Overlay detected regions of interest on a plate image}
\usage{
plot_grayleafspot_overlay(
  run,
  id = NULL,
  image_path = NULL,
  show_dish = TRUE,
  show_colony = TRUE,
  show_cracks = TRUE,
  dish_col = "yellow",
  colony_col = "red",
  crack_col = "cyan"
)
}
\arguments{
\item{run}{A \code{grayleafspot_run} object (see \code{\link[=grayleafspot_analyze]{grayleafspot_analyze()}},
\code{\link[=read_grayleafspot_results]{read_grayleafspot_results()}}), a plain list of per-image raw result
records, or a single record (a list with a \code{rawAnalysis} element, as
produced internally by the analysis engines).}

\item{id}{Optional character. \code{id} or \code{filename} of the image to overlay,
matched against the records in \code{run}. Defaults to the first record.}

\item{image_path}{Optional character. Explicit path to the source image.
Falls back to the record's stored \code{imageUrl}, which is only a resolvable
local file path for runs produced by the pure-R fallback engine; for
runs produced by the Python/basilisk pipeline, pass this explicitly
(e.g. a file inside the original \code{input_dir}).}

\item{show_dish, show_colony, show_cracks}{Logical. Which overlays to draw.}

\item{dish_col, colony_col, crack_col}{Character. Colors for each overlay,
passed to \code{\link[grDevices:col2rgb]{grDevices::col2rgb()}}.}
}
\value{
An \link[EBImage:Image]{EBImage::Image} with the requested overlays drawn on it.
Display with \code{EBImage::display()} or save with \code{EBImage::writeImage()}.
}
\description{
Draws the detected dish boundary, colony outline, and any detected crack
segments on top of the original plate photograph, using
\code{\link[EBImage:readImage]{EBImage::readImage()}} and EBImage's drawing primitives. This lets you
visually verify that automated segmentation and crack detection were
computed as expected, rather than trusting the summary statistics alone.
}
\examples{
img_dir <- system.file("extdata", "testdata", "06FEB", package = "grayleafspotr")
image_file <- list.files(img_dir, full.names = TRUE)[1]
record <- grayleafspotr:::analyze_grayleafspot_image(image_file)
overlay <- plot_grayleafspot_overlay(record)
}
